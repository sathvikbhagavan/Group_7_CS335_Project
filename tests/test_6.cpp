struct A{
    int x;
};

int main() {
    int x = -1;
    if(x == 1 || x == -1) {
        if (x > 0) {
            output("Yes!");
        }
        else {
            output("No!");
        }
    }
    return 0;
}