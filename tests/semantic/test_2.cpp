int func(int a,int c,int z)
{
    int b = 10;
    return 5 * b;
}

int main(int arv, char str)
{
    int x = 1, y, k;
    for (x = 1; x == y; x = x + 1)
    {
        k = k + y;
    }
    x = func(1,x,1);
    return 0;
}